meta {
  name: Create User - Missing Email
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "No Email User"
  }
}

tests {
  test("status code is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });

  test("response has error property", function() {
    const body = res.getBody();
    expect(body).to.have.property("error");
  });

  test("error mentions required fields", function() {
    const body = res.getBody();
    expect(body.error).to.equal("name and email are required");
  });

  test("no id is returned in error response", function() {
    const body = res.getBody();
    expect(body).to.not.have.property("id");
  });
}
